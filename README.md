Cursory is a Node.js utility for generating human-like mouse trajectories using a curated dataset of recorded paths, exposing a CommonJS API that returns a realistic list of `{x, y}` points between two coordinates with optional timing controls for sampling frequency and jitter, so you can integrate natural movement paths into automation workflows or UI simulations with minimal setup.
